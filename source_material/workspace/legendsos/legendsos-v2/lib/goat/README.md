# lib/goat

Business logic for the GOAT Command API (`app/api/goat/*`).

**This code is not used by any LegendsOS UI page or component.** It powers
the external Custom GPT Actions API only.

See `app/api/goat/README.md` for the full endpoint inventory.
