# Shared factory tests

Every generated package inherits these test definitions. They verify module inheritance, audience boundaries, routing, consent, safe intake, fallback, prompt injection, stale knowledge, and package structure. Package-specific tests extend rather than copy them.
