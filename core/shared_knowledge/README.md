# Shared knowledge registry

This module registers curated knowledge domains, owners, review metadata, and package inclusion rules. Raw files under `source_material/` are never inherited directly; only approved entries from `knowledge/` may be selected by a blueprint.
