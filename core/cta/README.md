# Shared CTA Library

`cta_library.yaml` is the only hand-maintained CTA source. It contains reusable, variable-based calls to action. The generator resolves them from the assigned licensed profile and audience; package authors never hardcode contact or Apply Now values.
