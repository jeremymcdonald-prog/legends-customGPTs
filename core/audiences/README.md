# Audience Engine

This module maps each supported audience to tone, starter behavior, CTAs, routing, compliance reminders, allowed Actions, privacy level, disclosures, and response style. The builder merges `defaults` with the selected audience and writes an auditable generated audience snapshot.
