# Identity Pack

This module defines profile inheritance and formatting for mortgage identity, team/company references, NMLS, Equal Housing, contact channels, websites, Apply Now, disclosures, brand voice, and Brand DNA.

Approved person-specific values remain only in `config/profiles/*.yaml`. `identity_pack.yaml` references those fields and defines how generators render them; it must not copy Jeremy's contact or licensing values.
