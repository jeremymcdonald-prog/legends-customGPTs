# Factory templates

This module contains the canonical blueprint, manifest schema/template, and package text templates used by the builder. `gpts/templates/` is the human entry point and references these sources; it does not maintain separate copies.
