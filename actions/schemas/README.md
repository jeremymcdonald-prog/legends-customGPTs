# Action schemas

Package- or platform-specific reviewed schemas may be exported here. Canonical reusable contracts live with their core module; the mortgage lead contract is `core/lead_capture/openapi.yaml`, with a compatibility link under `actions/lead_capture/`.

Schemas must use placeholder server URLs until approved, define authentication without embedding secrets, minimize returned fields, document consequential operations, and identify required human approval.
