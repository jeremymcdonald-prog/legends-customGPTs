# Action schemas

Store reviewed OpenAPI schemas here. Schemas must use placeholder server URLs, define authentication without embedding secrets, minimize returned fields, document consequential operations, and identify required human approval.
