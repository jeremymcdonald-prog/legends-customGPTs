# Planned GPTs

No individual GPT package was created during the foundation or Factory refactor. Approved concepts receive a blueprint first, then `scripts/build_gpt_package.rb` generates `gpts/<slug>/` only after build authorization. This folder may hold short approved business briefs, never copied core modules.
