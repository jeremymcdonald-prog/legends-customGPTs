# Next actions

## Recommended first Factory build

Authorize a bounded blueprint for **Legends Pipeline & Processing Copilot v0.1** as the first individual GPT generated by the Factory.

Use the `Internal Team` audience, synthetic data, `public_or_internal: internal`, `lead_capture_enabled: false`, `apply_now_behavior: no_application_link`, no browsing, no public mortgage CTA, and read-only pipeline capability only after the authenticated data owner approves it. This remains the strongest first build because the source design is mature, usage is frequent, value is measurable, and consumer risk can stay outside the pilot.

## Before that build

1. Name business, knowledge, compliance, security/data, technical, and launch owners.
2. Curate and approve the minimum pipeline knowledge; do not mount raw source material.
3. Write the blueprint and package-specific acceptance tests before enabling any Action.
4. Generate the package with the Factory and verify its dependency fingerprint and inherited controls.
5. Pilot with synthetic data and two or three internal users; measure time saved, correction rate, and scope failures.

## Later public lead-capture gates

Select the system owner, destination adapter, valid public privacy policy, retention/deletion schedule, threat review, licensing source, consent version, monitoring, rollback, and human follow-up SLA before implementing or enabling `submitMortgageLead`.
